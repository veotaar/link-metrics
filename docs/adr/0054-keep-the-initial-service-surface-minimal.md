# Keep the initial service surface minimal

The first API Contract contains only health readiness, User registration, login, Short Link creation, public resolution, and owned statistics. Listing, editing, deletion, custom Short Codes, logout, refresh tokens, and aggregate analytics are deliberately excluded because they expand product behavior without improving the initial backend-stack comparison.
