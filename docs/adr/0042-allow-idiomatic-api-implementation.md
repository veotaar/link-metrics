# Allow idiomatic API implementation

Contenders may use framework-appropriate routing, DTO, validation, serialization, and OpenAPI generation techniques rather than a common code generator. Any generated derivatives stay local and follow a committed-and-regenerated lifecycle, while mandatory black-box conformance—not shared runtime validation code—enforces the API Contract and keeps those framework layers inside the complete stack comparison.
