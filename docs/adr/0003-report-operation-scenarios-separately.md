# Report operation scenarios separately

Primary benchmark results are reported separately for registration, login, authenticated link creation, public redirect, and authenticated statistics lookup. A mixed-traffic scenario may supplement those results but cannot replace them, because a blended score would be dominated by its workload proportions and could conceal costs such as password hashing or JWT processing.
