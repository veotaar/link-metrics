# Benchmark complete backend stacks

Link Metrics compares production-plausible backend stacks rather than isolating individual runtimes or frameworks. Every contender must satisfy the same externally observable API contract, database schema, dataset, and workload, but may use idiomatic internal libraries and techniques; consequently, results describe the performance of the complete stack and cannot attribute differences to any single component.

