# Do not publish a composite ranking

The benchmark publishes per-scenario saturation curves and standardized maximum sustainable throughput together with latency and error rates, but no official composite score or overall winner. Any composite would encode arbitrary traffic weights between fundamentally different operations and could reverse contender rankings without a performance change.
