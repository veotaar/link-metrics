# Benchmark Dataset version

`VERSION` identifies the deterministic PostgreSQL state restored before every Trial. Schema or Dataset-content changes increment this version independently of the API Contract and benchmark protocol.
