# Benchmark fixtures

Fixtures in this directory are public, reproducible benchmark inputs. They are not
operational credentials and must never be substituted for production secrets.

`jwt-hs256.key` is the fixed 32-byte key used to issue reference tokens and to verify
that every Contender implements the standard JWT profile.
